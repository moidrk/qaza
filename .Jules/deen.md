2024-06-11 - QazaDetailSheet Touch Targets & Loading States\nLearning: Interactive elements in the QazaDetailSheet were too small for comfortable mobile use (under 44px) and lacked loading/disabled states during API calls, leading to potential duplicate submissions and poor UX.\nAction: Enforce a minimum touch target size of 44px (e.g., using Tailwind's min-h-[44px]) for all interactive buttons and inputs. Implement explicit loading and disabled states for async actions to provide immediate visual feedback.
2024-06-16 - PrayerList Mobile Button Targets
Learning: Daily prayer list buttons were cramped and hard to hit on mobile screens because they were constrained to a single row without expanding flex properties.
Action: Used `flex-col sm:flex-row` and `flex-1`/`flex-[2]` properties on action buttons inside list cards to allow them to stack or take full width horizontally on mobile screens, significantly improving touch usability while keeping them intact for desktop. Shortening labels (e.g., "Missed" -> "Miss") on small screens helps fit content.
