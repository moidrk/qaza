2024-06-11 - QazaDetailSheet Touch Targets & Loading States\nLearning: Interactive elements in the QazaDetailSheet were too small for comfortable mobile use (under 44px) and lacked loading/disabled states during API calls, leading to potential duplicate submissions and poor UX.\nAction: Enforce a minimum touch target size of 44px (e.g., using Tailwind's min-h-[44px]) for all interactive buttons and inputs. Implement explicit loading and disabled states for async actions to provide immediate visual feedback.
2024-10-27 - PrayerList Mobile Layout & Touch Targets
Learning: The daily prayer cards used an inline layout (`justify-between`) that caused primary action buttons to become cramped and difficult to tap on mobile screens. Additionally, secondary buttons relied entirely on icons without clear text labels, violating accessibility and UX standards.
Action: Enforce stacked layouts (`flex-col sm:flex-row`) for interactive cards on mobile. Ensure action buttons span the full width of the container on narrow screens (`w-full`) to provide massive touch targets, and always use explicit text labels (e.g., "Miss") alongside icons.
